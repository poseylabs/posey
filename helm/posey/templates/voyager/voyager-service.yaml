apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-voyager
  labels:
    app: {{ .Release.Name }}-voyager
    chart: {{ .Chart.Name }}-{{ .Chart.Version }}
    release: {{ .Release.Name }}
spec:
  type: ClusterIP
  ports:
    - port: 3030
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ .Release.Name }}-voyager 