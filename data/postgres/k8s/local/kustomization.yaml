apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: posey

resources:
  - postgres-statefulset-local.yaml
  - postgres-secret.yaml
  - postgres-configmap.yaml
  - postgres-storage.yaml
  - postgres-service.yaml 